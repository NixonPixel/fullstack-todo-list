import { withStyles } from "@material-ui/core";

const classes = withStyles(() => ({
    error: {
        color: 'tomato',
        textAlign: 'center',
        display:' block',
    }
}))

export default classes